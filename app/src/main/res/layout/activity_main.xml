<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:bind="http://schemas.android.com/tools"
    tools:context=".MainActivity">

    <data>
        <variable
            name="viewModel"
            type="com.example.ourstocktest.ViewModel" />
        <import type="android.view.View"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!--<ProgressBar
            android:visibility="@{viewModel.loading ? View.VISIBLE : View.GONE}"
            android:id="@+id/pbLoading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>-->

        <EditText
            android:id="@+id/etPhone"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@={viewModel.phone}"
            android:inputType="phone"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginTop="20dp"
            android:layout_marginHorizontal="20dp"/>
        <!-- 전화번호가 9자~12자 사이의 길이일 때만 활성화됨. -->
        <Button
            android:enabled="@{9 &lt;= viewModel.phone.length() &amp;&amp; viewModel.phone.length() &lt;= 12}"
            android:clickable="@{9 &lt;= viewModel.phone.length() &amp;&amp; viewModel.phone.length() &lt;= 12}"
            android:focusable="@{9 &lt;= viewModel.phone.length() &amp;&amp; viewModel.phone.length() &lt;= 12}"
            android:id="@+id/btnLogin"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="로그인"
            android:onClick="@{()->viewModel.onLoginButtonClick()}"
            app:layout_constraintTop_toBottomOf="@+id/etPhone"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            android:layout_marginTop="20dp"/>
        <!--"@{viewModel.response.callNumber + ' ' + viewModel.response.username}"-->
       <TextView
            android:id="@+id/tvInfo"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:text="@{viewModel.response.callNumber + '\n' + viewModel.response.username + '\n' + viewModel.response.userId}"
            app:layout_constraintTop_toBottomOf="@+id/btnLogin"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_marginTop="20dp" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>